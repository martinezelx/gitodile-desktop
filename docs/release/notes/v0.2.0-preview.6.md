# GitOdile 0.2.0-preview.6

First production-key preview reserved for the future Windows x86-64 NSIS and
Linux x86-64 AppImage public qualification sequence. macOS remains disabled.

Publication requires the production updater identity and remaining public-
delivery gates. Windows Authenticode is deliberately deferred until after
`1.0.0` and the download must disclose the unknown-publisher warning.
